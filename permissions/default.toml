"$schema" = "schemas/schema.json"

[default]
description = """
This permission set configures the default share functionality.

#### Granted Permissions

Allows sharing content via the native share sheet and checking 
if sharing is available on the current platform.
"""

permissions = [
  "allow-share",
  "allow-can_share",
]
